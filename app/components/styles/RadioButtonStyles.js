import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  radioContainer: {
    flexDirection: 'row',
    marginBottom: 10
  },
  radio: {
    height: 20,
    width: 20
  },
  radioTitle: {
    marginLeft: 10
  }
});

export default styles;
